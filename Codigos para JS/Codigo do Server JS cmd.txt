<!-- npm init -y PARA CRIAR UMA DAS NOSSAS PRIMEIRA DEPENDENCIA(json) executar no terminal pront de command -->
 <!-- npm install express mysql2 bcrypt body-parser cors ESSE COMANDO É O pacote que lida com a segurança de senhas é o bcrypt.
O mysql2 é utilizado para conectar sua aplicação a um banco de dados MySQL. -->
  <!-- Da o Start no mysql/ net start mysql / -->
<!-- CRIAR NOSSO BANCO DE DADOS mysql-u root -->
<!-- node server.js //COMANDO PARA ABRIR O HTTP do site -->

CREATE DATABASE IF NOT EXISTS sistema_login;

USE sistema_login;

CREATE TABLE usuarios (
    id INT AUTO_INCREMENT PRIMARY KEY,
    nome VARCHAR(100) NOT NULL,
    email VARCHAR(100) UNIQUE NOT NULL,
    senha VARCHAR(255) NOT NULL,
    data_criacao TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

